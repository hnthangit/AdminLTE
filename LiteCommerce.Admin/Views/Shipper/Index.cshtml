@model LiteCommerce.Admin.Models.ShipperResult
@{
    ViewBag.Title = "Shipper";
}

<div class="box box-info">
    <div class="box-body">
        <form action="~/Shipper" method="get">
            <div class="input-group input-group-sm">

                <input name="searchValue" type="text" class="form-control" placeholder="Input search values...." value="@Model.searchValue"> 
                <span class="input-group-btn">
                    <button type="submit" class="btn btn-info btn-flat">
                        <i class="fa fa-search"></i>
                    </button>
                </span>
            </div>
        </form>
    </div>

</div>
<div class="container-fluid row" style="margin-bottom:10px">
    <div class="col-sm-8">
        There are <b>@Model.RowCount</b> Shipper found. Page @Model.Page/ @Model.PageCount.
    </div>
    <div class="col-md-4 text-right">

        <div class="btn-group">
            <a href="~/Shipper/Input" class="btn btn-sm btn-success">Add Shipper</a>
            <button type="button" class="btn btn-sm btn-danger">Delete Shipper</button>
        </div>

    </div>
</div>
<div class="box box-success">
    <div class="box-body">
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th class="sw-30">
                        <input type="checkbox" />
                    </th>
                    <th class="sw-50">#</th>
                    <th class="sw-auto">Company Name</th>
                    <th class="sw-auto">Phone</th>
                    <th class="sw-100"></th>

                </tr>
            </thead>

            <tbody class="text-align">
                    @foreach(var item in Model.Data)
                    {

                        <tr>
                            <td>
                                <input type="checkbox" />
                            </td>
                            <td>
                                @item.ShipperID
                            </td>
                            <td>
                                <span class="text-primary cnp-name">@item.CompanyName</span>
                            </td>
                            <td>
                                <span>@item.Phone</span>
                            </td>
                            <td class="text-center">
                                <!-- Button trigger modal -->
                                <a href="~/Shipper/Input/@item.ShipperID" class="btn btn-primary center-block">
                                    <i class="fa fa-info"></i>
                                </a>


                            </td>
                        </tr>
                    }
                

            </tbody>
        </table>        
    </div>
</div>

